"""FlightScope backend package.

Server-side logic isolated from apps/app_flightscope_unified.py. Contains no
Streamlit / UI code. Every function and computation is preserved unchanged from
the original unified app; only structural separation was performed.
"""
